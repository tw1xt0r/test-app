import type { ServerOptions } from "ws";
import { CssToReactNativeRuntimeOptions, ComposableIntermediateConfigT } from "react-native-css-interop/metro";
interface WithNativeWindOptions extends CssToReactNativeRuntimeOptions {
    input?: string;
    projectRoot?: string;
    outputDir?: string;
    configPath?: string;
    hotServerOptions?: ServerOptions;
    cliCommand?: string;
}
export declare function withNativeWind(metroConfig: ComposableIntermediateConfigT, { input, outputDir, projectRoot, inlineRem, configPath: tailwindConfigPath, cliCommand, hotServerOptions, }?: WithNativeWindOptions): ComposableIntermediateConfigT;
export {};
