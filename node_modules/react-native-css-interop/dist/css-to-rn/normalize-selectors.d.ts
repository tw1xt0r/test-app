import type { SelectorList } from "lightningcss";
import { CSSSpecificity, ExtractRuleOptions, ExtractedStyle } from "../types";
export type NormalizeSelector = {
    type: "rootVariables" | "universalVariables";
    subtype: "light" | "dark";
} | {
    type: "className";
    darkMode?: boolean;
    className: string;
    groupClassName?: string;
    pseudoClasses?: Record<string, true>;
    groupPseudoClasses?: Record<string, true>;
    specificity: Pick<CSSSpecificity, "A" | "B" | "C">;
};
export declare function normalizeSelectors(extractedStyle: ExtractedStyle, selectorList: SelectorList, options: ExtractRuleOptions, selectors?: NormalizeSelector[], defaults?: Partial<NormalizeSelector>): NormalizeSelector[];
